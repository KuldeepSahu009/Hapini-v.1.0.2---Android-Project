<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.crm.pvt.hapinicrm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HapiniAllInOne">
        <activity android:name=".Homescreen"/>
        <activity android:name=".Admin.Videoeditoradmin"/>
        <activity android:name=".Admin.Onlyformasterlogin" />
        <activity android:name=".Admin.Admingenerallogin" />
        <activity android:name=".resetpassword.forgetpassword" />
        <activity android:name=".resetpassword.newpassword" />
        <activity android:name=".resetpassword.confirmotp" />
        <activity android:name=".Admin.Crmadmin" />

        <activity android:name=".Admin.Dataentryadmin" />
        <activity android:name=".Admin.MasterAdmin" />
        <activity android:name=".Admin.workselectadmin" />
        <activity android:name=".splashscreen"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>